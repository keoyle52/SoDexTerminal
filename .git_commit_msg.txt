fix: SosoValue API critical bug fixes

- Fixed coin list field names: currencyId -> id, currencyName -> name
- Fixed categoryList URL encoding: URLSearchParams encodes commas as %2C
  but API expects literal commas — added buildQuery helper to bypass this
- Fixed error messages: extract .msg/.message from both body and HTTP errors
- Improved error interceptor to format 401/429 errors clearly
- Fixed CryptoNews: replaced fragile ref-based init with clean useEffect on sosoApiKey
- EtfCurrentMetrics now nullable to match updated service signature
