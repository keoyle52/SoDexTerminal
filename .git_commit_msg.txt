fix: resolve 429 rate limit errors with TTL cache and StrictMode removal

- Remove React StrictMode to stop effects firing twice in development
- Add TTL in-memory cache to sosoValueClient:
    coins: 5 min, ETF data: 3-5 min, news: 2 min
- Cache uses custom Axios adapter to bypass network on cache hits
- Refresh buttons in EtfTracker and CryptoNews now call clearSosoCache()
  to force fresh data when user explicitly requests it
- Users navigating between pages no longer hit new API requests
